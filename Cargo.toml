[workspace]
resolver = "2"
# The shell is a workspace member so that it builds into the same target
# directory as the agent. The agent only serves clients installed alongside it,
# so co-located build output is what lets `cargo run` exercise that check
# instead of tripping over it.
members = ["crates/*", "ui/src-tauri"]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
authors = ["KAM Security"]
repository = "https://github.com/OWNER/kam-security"

[workspace.dependencies]
kam-core = { path = "crates/kam-core" }
kam-ipc = { path = "crates/kam-ipc" }
kam-scanner = { path = "crates/kam-scanner" }
kam-firewall = { path = "crates/kam-firewall" }
kam-storage = { path = "crates/kam-storage" }
kam-quarantine = { path = "crates/kam-quarantine" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

rusqlite = { version = "0.32", features = ["bundled"] }

windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Authorization",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
    "Win32_System_Memory",
    "Win32_System_Pipes",
    "Win32_System_Threading",
] }

# Added at the phase that first needs them.
# windows-service = "0.7"
# blake3 = "1"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_debug_implementations = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"
